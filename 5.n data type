# 5.n data type

**불리언 타입**

![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/73cd62e8-db8d-4181-b090-9c7257038236/0ffda10c-981e-474f-9a6d-dbc32abe5005/Untitled.png)

![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/73cd62e8-db8d-4181-b090-9c7257038236/cb2ec0c3-64b8-4c4d-abf5-5ba2c2ba744d/Untitled.png)

불리언 타입의 값은 참과 거짓으로 구분되는 조건의 의해 

프로그램의 흐름을 제어하는 조건문에 자주 사용한다.

**undefined타입**

: 값은 undefined가 유일하다.

전에 말했듯이var 키워드로 선언한 변수는 암묵적으로 undefined로 초기화된다.

따라서 변수를 선언한 이후 값을 할당하지 않은 변수를 참조하면 undefined가 반환 된다.

변수를 참조 했을떄 undefined가 반환 된다면 참조한 변수가 선언 이후 값이

할당된 적이 없는 초기화되지 않은 변수라는 것을 간파 할 수 있다.

변수에 값이 없다는 것을 명시하고 싶을 떄는 undefined를 할당하는 것이 아닌 null을 할당한다.

**null타입**

: 값은 null이 유일하다. 자바스크립트는 대소문자를 구별하므로 null, Null, NULL등 전부다 다르다.

null은 변수에 값이 없다는 것을 의도적으로 명시 할떄 사용한다.

변수에null을 할당하는 것은 변수다 이전에 참조하던 값을 더 이상 참조하지 않겠다는 의미이다

자바스크립트 엔진은 누구도 참조하지 않은 메모리 공간에 대해 가비지콜렉션을 수행 할 것이다.

함수가 유효한 값을 반환할수없는경우 명시적으로 null을 반환하기도 한다.

**심벌 타입**

: 심벌은 Es6 에서 추가된 7번쨰 타입으로, 변경이 가능한 원시 타입의 값이다.

심벌 값은 다른 값과 중복되지 않은 유일무이한 값 이다.

따라서 주로 이름이 충돌할 위험이 없는 객체의 유일한 프로퍼터 키를 만들기 위해 사용한다

심벌은 Symbol이 함수를 호출해 생성한다.

이떄 생성된 심벌 값은 외부에 노출되지 않으며 다른 값과 절대 중복되지 않는 유일무이한 값이다

**객체 타입**

: 자바스크립트의 데이터 타입은 크게 원시 타입과 객체 타입으로 분류한다고 했다.

이 두 개의 타입은 근본적으로 다르다는 것이다

자바스크립트는 객체 기반의 언어이다

( 자바스크립트가 이루고 있는 거의 모든 것들은 객체라는 것이다)

**데이터 타입의 필요성**

**데이터 타입의 의한 메모리 공간의 확보와 참조.**

값의 메모리를 정하고 참조 할 수 있어야 한다.

메모리 값을 저장 하려고 하면 먼저 확보해야 할 메모리의 공간의 크기를 지정해야 한다

즉 몇 바이트의 메모리 공간을 사용해야 낭비와 손실 없이 값을 지정 할 수 있는지 알아야 한다.

![Untitled](https://prod-files-secure.s3.us-west-2.amazonaws.com/73cd62e8-db8d-4181-b090-9c7257038236/884cc96a-38e2-42d0-98ff-d2394090434c/Untitled.png)

숫자 값 200을 지정하기 위해 메모리 공간을 확보한 다음 확보된 메모리에 숫자 값 200을 2진수로 저장한다.

숫자 값을 저장 할 때 확보해야 할 메모리 공간의 크기를 알아야 한다

자바스크립트의 엔진은 데이터 타입, 값의 종류에 따라 정해진 크기의 메모리 공간을 확보 해야 한다.

값을 참조 하는 경우 식별자 score 을 통해 숫자 타입의 값 200 이 저장되어 있는 메모리 공간 주소를 찾아 갈 수 있다 이떄 값을 참조 하려면 한번에 읽어 들여야 할 메모리 공간의 크기, 즉 메모리 셀의 개수를 알아야 한다.

score 변수의 경우 저장 되어 있는 값이 숫자 타입 이므로 

8비트 단위로 읽어드리지 않으면 값이 훼손 된다

**궁금증**

**컴퓨터가 한번에 읽어 들여야 할 메모리 공간 크기를 아는 방법은??**

변수에는 숫자 타입의 값이 할당되어 있으므로 (자바스크립트는 score변수를 숫자 타입으로 인식)

숫자 타입은 8비트로 저장되어있으므로 score 변수를 참조하면 8비트 단위로 메모리 공간에 저장된 값을 읽어준다.

**데이터 타입의 의한 값의 해석**

:메모리에 읽어드린 2진수를 어떻게 해석 해야 하는가.

모든 값은 데이터 타입을 가지며 메모리에 2진수, 즉 비트의 나열로 저장된다.

메모리에 저장된 값은 데이터 타입에 따라 다르게 해석할 수 있다.

score변수를 참조하면 메모리 공간의 주소에서 읽어드린 2진수를 해석한다 .

데이터 타입의 정리

- 데이터 타입은 값의 종류를 말한다
- 자바스크립트의 모든 값은 데이터 타입을 갖는다

?데이터 타입이 필요한 이유?

- 값을 저장할 떄 확보해야 하는 메모리 공간의 크기를 결정하기 위해
- 값을 참조할 떄 한번에 읽어 들여야 할 메모리 공간의 크기를 결정하기 위해
- 메모리에서 읽어 드린 2진수를 어떻게 해석할지 결정하기 위해
